# BaseModule

## Purpose
Holds the pieces every FlowIoC module is built on: Root, Context, injection, signals and commands.

## Concepts
root, context, binding, injection, signal, command, sequence, mediator, view
