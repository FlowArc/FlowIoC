namespace FlowIoC.AssetModule.Constants
{
    public static class AssetConstants
    {
        // Bootstrapped AssetService must initialize before any consumer context.
        public const int InitializeOrder = -10000;
    }
}
