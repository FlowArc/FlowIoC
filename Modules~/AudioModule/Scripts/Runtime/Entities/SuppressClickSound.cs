using UnityEngine;

namespace Modules.AudioModule.Entities
{
    /// <summary>
    /// Put on a button - or anywhere above it - that plays a sound of its own, so the click sound
    /// the Audio module plays for every button leaves it alone.
    /// </summary>
    public class SuppressClickSound : MonoBehaviour
    {
    }
}
