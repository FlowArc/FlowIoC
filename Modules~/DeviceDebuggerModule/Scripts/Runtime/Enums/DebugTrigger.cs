namespace Modules.DeviceDebuggerModule.Enums
{
    /// <summary>How the panel is opened from the screen. None leaves only the service and the error badge.</summary>
    public enum DebugTrigger
    {
        Button = 0,
        TripleTap = 1,
        None = 2
    }
}
