namespace Modules.DeviceDebuggerModule.Enums
{
    /// <summary>Where the trigger and the error badge sit.</summary>
    public enum DebugCorner
    {
        TopLeft = 0,
        TopRight = 1,
        BottomLeft = 2,
        BottomRight = 3
    }
}
