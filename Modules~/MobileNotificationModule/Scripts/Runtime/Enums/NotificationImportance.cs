namespace Modules.MobileNotificationModule.Enums
{
    /// <summary>An Android channel's importance. iOS has no channels and ignores it.</summary>
    public enum NotificationImportance
    {
        Low = 0,
        Default = 1,
        High = 2
    }
}
