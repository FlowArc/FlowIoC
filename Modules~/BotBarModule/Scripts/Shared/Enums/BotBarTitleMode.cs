namespace Modules.BotBarModule.Shared.Enums
{
    /// <summary>When a tab shows its title.</summary>
    public enum BotBarTitleMode
    {
        Always = 0,
        SelectedOnly = 1,
        Never = 2,
    }
}
